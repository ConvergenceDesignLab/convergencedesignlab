# Disable caching on HTML and JS files only
# <filesMatch ".(html|htm|js)$">
# 	FileETag None
# 	<ifModule mod_headers.c>
# 		Header unset ETag
# 		Header set Cache-Control "max-age=0, no-cache, no-store, must-revalidate"
# 		Header set Pragma "no-cache"
# 		Header set Expires "Wed, 08 Jan 1975 05:00:00 GMT"
# 	</ifModule>
# </filesMatch>

# Disable caching on HTML and JS files only
ExpiresActive On
ExpiresByType text/html "access plus 0 minutes"
ExpiresByType text/javascript "access plus 0 minutes"

# See remapping example: https://httpd.apache.org/docs/2.4/rewrite/remapping.html
RewriteEngine On
RewriteRule "^work/(.+)" "/project/index.html" [PT]
RewriteRule "^resources/(.+)" "/resource/index.html" [PT]
RewriteRule "^login" "https://api.convergencedesignlab.org/wp-admin/" [R,L]

ErrorDocument 404 /404.html